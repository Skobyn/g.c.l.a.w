.superpowers/
.claude/
__pycache__/
*.pyc
node_modules/
.next/
*.tsbuildinfo
.env
.env.local
web/.env.local
.firebaserc.local
tmp/

# Agent / orchestration tool artifacts (claude-flow, ruv-swarm, MCP)
.claude-flow/
web/.claude-flow/
.swarm/
.mcp.json
ruvector.db

# Dev-time Claude/SPARC working files
/schedule-loop*.txt

# Local debugging screenshots
/chat-page-screenshot.png

# Eval result files — local to a run, never committed
tests/eval/results/*.json

# Optional overlay repo co-located with the framework
/overlay/
/gclaw-overlay/

# Personal / brand-specific skills that ship only in the overlay.
# Add more lines here if you peel additional skills out to a private
# overlay via scripts/extract-personal-overlay.sh.
skills/lyons-blog-pipeline/

# IDE and OS
.vscode/
.idea/
.DS_Store
*.log

# Credentials and key material — never commit
*.pem
*.key
*.p12
*.pfx
*.jks
*credentials*.json
*service-account*.json
*-sa.json
application_default_credentials.json
